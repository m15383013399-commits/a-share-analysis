# 验收记录（2026-09-21）

- Python 单元与集成测试：223 项，209 通过，14 项因未分发个人历史台账而跳过。包含真实 CLI 注册链的合成预测测试：首次成功登记，同日不同内容拒绝覆盖并保留草稿。
- 前端 TypeScript 检查与 Vite 生产构建通过。
- Docker Web 健康检查通过，worker 在线；Linux ARM64 下 AkShare 导入成功。
- 浏览器实测管理员登录、离线演示提交/生成/阅读、资料采集状态更新。
- 2026-09-21 盘中收盘任务返回退出码 3、等待收盘；没有写入该日正式快照。
- 实测 600519 资料采集：250 条未复权日线、45 条财务摘要、10 条新闻；仅存本机 runtime，不随源码发布。数量表示接口返回覆盖，不表示数据已交叉核实或财务具备历史可见时间。
- 模型分析、审阅、修订与预测登记使用模拟模型返回做流程验证；没有用户模型密钥，未进行真实付费模型请求。黄金的有凭证 XAU/USD 路径未实测。
- runtime、账号凭据、模型密钥及个人行情台账均排除出 Git 和 Docker 构建上下文。

这份记录只说明已执行的检查，不保证第三方接口持续可用，也不保证模型研究正确。

## 2026-09-22：Codex CLI 引擎

- 新增本机执行服务与共享目录任务协议，界面可选择模型 API 或 Codex CLI；已保存的旧 API 配置兼容。
- 234 项自动测试：220 通过，14 项私人历史台账用例跳过。新增覆盖真实子进程的模拟 CLI 往返、结构化结果、缓存隔离、取消、容器租约丢失、超时、重复执行保护及无平台 API Key 的 CLI 入口。
- 在 Mac 登录状态下实测 Docker → 本机 Codex CLI 0.135.0 → Docker：连接测试成功。
- 使用已归档的 600519 资料完成真实 CLI 主分析与审阅；证据引用校验与审阅通过，报告保存成功。历史研究未写入正式预测。首次发现的证据编号错误被校验拦截，修复提示约束后重新验证通过。
- 浏览器验证了 API/CLI 选项切换、Codex 就绪状态、参数显示和保存；生产构建通过。
- 服务以当前用户 LaunchAgent 运行，不新增端口，不将 Codex 登录文件挂载进容器。CLI 参数以本机帮助为依据，AGENTS 文档读取限制参照 [OpenAI 配置参考](https://learn.chatgpt.com/docs/config-file/config-reference)。
